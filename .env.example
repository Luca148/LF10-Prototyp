# Copy to .env and fill in. The .env file is git-ignored.

# Cloudflare Tunnel token.
# Zero Trust dashboard -> Networks -> Tunnels -> your tunnel -> Configure ->
# "Install and run a connector" -> copy the value after `--token`.
TUNNEL_TOKEN=
